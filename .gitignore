# Downloaded databases (Scryfall bulk data, EDHREC cache) — too large for git
# and fully reconstructible via scryfall.py / mtgdb.py build / edhrec.py fetch
data/

# Real personal collection (decks, cards, games) — stays local only, never in the repo
webapp/app.db

# Python
__pycache__/
*.pyc
.DS_Store

# Rust / Tauri build output (regenerated by cargo; hundreds of MB)
desktop/src-tauri/target/
desktop/src-tauri/gen/
webapp/app.db.migrated-to-appsupport
